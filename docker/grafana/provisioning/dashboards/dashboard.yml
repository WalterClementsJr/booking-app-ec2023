apiVersion: 1

providers:
  - name: 'Prometheus'
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /var/lib/grafana/dashboards

#TODO
#sum(jvm_memory_used_bytes)/1000000
#sum(rate(system_cpu_usage[10m]))
